interface:
  display_name: "山音超级编剧大师"
  short_description: "Create screenplays with Shanyin's official four-format method"
  default_prompt: "Use $wally-screenplay-writer for the requested story, screenplay, dialogue, voiceover, or diagnosis task. Continue from the supplied material, select only useful methods and supported fields, and write less when uncertain."

policy:
  allow_implicit_invocation: false
