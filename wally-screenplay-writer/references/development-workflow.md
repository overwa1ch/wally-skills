# 分步开发细则

用户选择分步开发、要求锁定或需要长内容检查点时，读取当前相关章节。任务范围与已有材料处理以 [SKILL.md](../SKILL.md) 的工作流纪律为准。

## 叙事类通用八步流程

1. 破题与核心动作
2. 梗概草稿
3. 人物深度与弧光
4. 前史与世界观
5. 结构大纲
6. 场景拆解
7. 场景写作
8. 剧本医生

叙事短片、长片和剧集单集共用以上方法索引。是否展开为八次交付，按用户选择的工作方式决定。

### 八步基础交付合同

用户选择分步开发时，下表帮助确定每步的核心产物；字段只按当前需求展开，定向任务使用相关部分：

| 当前步骤 | 基础交付 |
| --- | --- |
| 破题与核心动作 | 核心戏剧动作（目标 → 阻碍 → 结果或失败代价）与一句话 Logline |
| 梗概草稿 | 让用户能判断整体走向和气质的梗概；长度按格式缩放 |
| 人物深度与弧光 | 主要人物的 Want、Need、内在阻碍、关系张力与 A → B 弧光 |
| 前史与世界观 | 时代背景、关键前史、关系既往、必要世界规则与横截面选择 |
| 结构大纲 | 结构节点、开场钩子、高潮、结局与分段时长预算 |
| 场景拆解 | 按场景列出戏剧动作、结果、时长和双轨节奏 |
| 场景写作 | 可拍、可听的标准剧本正文，以及角色可区分且有潜台词的对白 |
| 剧本医生 | 按用户范围交付诊断；用户要求改写时同时给可直接替换的改写稿 |

长片和剧集单集在本表基础上叠加各自 reference 的当前步骤要求，不需要借用叙事短片 reference 才能补全基础输出。

### 格式缩放

以下是方法的组织方式；是否分步交付、展示分析或使用菜单，仍按“工作流纪律”决定。

- **概念超短片是三步例外：** 概念锻造或形式发现 → 结构与视听设计 → 全片剧本。人物、前史/世界观和场景拆解不另设步骤。
- **叙事短片：** 用八步索引定位当前工作，围绕核心事件取舍人物和场景。Want / Need / Arc 用于确实需要人物发展的故事，前史与世界观只补当前所需。
- **长片：** 用八步索引组织开发，按故事需要选用 Treatment、人物前史、Sequence、Subplot、伏笔和暗线。
- **剧集：** 阶段 A 规划季度，阶段 B 拟定分集大纲，阶段 C 进入单集工作；已有材料直接进入对应阶段。逐集步骤2和步骤4通常是对照确认，实际发展偏离大纲时才更新。

### 统一审批菜单

仅在用户已选择分步开发或明确要求锁定时使用本节。参考资料中的审批提示沿用此条件。

采用分步开发时，非最终步骤或分批交付结束后，输出当前产物和以下菜单并停止。`[通过]` 接受当前交付：当前步骤未完成时继续同一步的下一批，当前步骤已完成时才进入下一步。

```text
[通过]：接受当前交付并继续
[修改]：留在当前步骤修改
[自检]：查看当前步骤检查结果
```

分步开发到达最终交付，或用户要求锁定当前稿时，使用以下菜单。无需完整格式路由的独立定向对白、旁白、场景改写或诊断，只有用户要求锁定时才使用：

```text
[通过并锁定]：完成并锁定当前交付范围
[修改]：继续精修当前交付范围
[自检]：查看当前步骤检查结果
```

锁定只作用于本轮明确交付的范围：完整终稿会锁定整部当前剧本，定向对白或局部场景改写只锁定相应段落，范围外内容的批准状态不变。

格式 references 只定义专业方法和该步骤的输出，不得复制或改写审批菜单；菜单文字以本文件为唯一来源。

## 长内容功能

- 长片和剧集默认考虑伏笔/回扣、次要角色/群像、暗线与 Subplot；详见对应格式 reference。
- 时长预算和双轨节奏用于需要容量规划或节奏判断的任务；缺少依据时不凑精确秒数，普通修改不附加整套分析。
- 长片和剧集按“表层规则 → 运行逻辑 → 底层真相”管理世界观信息释放。
- 剧集维护角色状态、暗线进度、埋种/回调和已公开信息。
- 长片每完成一个 Sequence、剧集每完成一集时按 [core-methodology.md](core-methodology.md) 的“十、记忆检查点系统”生成检查点；单集超过6场时增加集内检查点。
