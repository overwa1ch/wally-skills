# Route B Final Prompt Template

```text
SEGXX

Asset List:
“[可直接指代画面对象的身份名]” = [已确认平台引用，如 @图片1] - [资产类型；控制内容]。

Prompt:
[粘贴完整 Director 正文并保留其实际字段与执行载体。在资产实际可见或控制画面的 Subject、Action、Timing/beats 或 Shot 中直接写入对应的“对象身份名”；只在 Continuity、说明或外层清单中引用不算完成。除最短资产引用外，其余内容不变。平台引用不进入 Prompt。正文含末尾 Avoid: 或“避免：”时删除该字段并移至 Constraints；正文不含时不补。]
[命名唯一性：按 artifact-compatibility.md 的 One asset, one name 规则执行——全文只用 Asset List 绑定的带引号身份名，替换一切近义别名，部件一个规范名，未绑定进本 SEG 的资产不点名。]

Constraints:
只使用本SEG Asset List中的成品资产；不得把参考板内部局部或状态拆成独立资产。
[含已批准的Voiceover或画外发声：不得添加正文之外的旁白、画外解说。否则：无旁白、无画外解说。]
[含Voiceover、Dialogue或其他发声：画面中不得生成文字、字幕或对白气泡。]
[正文含 Avoid: 或“避免：”时：禁止出现：[原字段内容。]；正文不含时省略本行。]
```
